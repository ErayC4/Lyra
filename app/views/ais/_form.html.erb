<%= form_with(model: ai) do |form| %>
  <% if ai.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ai.errors.count, "error") %> prohibited this ai from being saved:</h2>

      <ul>
        <% ai.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :generation, style: "display: block" %>
    <%= form.text_field :generation %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
